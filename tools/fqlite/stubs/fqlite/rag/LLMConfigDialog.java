package fqlite.rag;

import javafx.application.Application;
import javafx.stage.Stage;

/**
 * Compile-only stub for the headless fqlite tap (see {@link LLMWindow}).
 * Referenced only by GUI.java's {@code showConfigDialog()}, never reached headlessly.
 */
public class LLMConfigDialog extends Application {

    @Override
    public void start(Stage primaryStage) {
        throw new UnsupportedOperationException("fqlite.rag stubbed out in headless tap");
    }
}
